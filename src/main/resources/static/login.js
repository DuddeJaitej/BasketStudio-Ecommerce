const form=document.querySelector('#loginForm');form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(!response.ok){document.querySelector('#loginError').textContent='Email or password is incorrect.';button.disabled=false;return}const user=await response.json();localStorage.setItem('basketToken',user.token);location.assign('/profile.html')});
